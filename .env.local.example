# Azure DevOps Configuration
# Copy this file to .env.local and fill in your actual values

# Your Azure DevOps organization name (from the URL)
# For https://cmgfidev.visualstudio.com/... use: cmgfidev
# For https://dev.azure.com/yourorg/... use: yourorg
NEXT_PUBLIC_ADO_ORGANIZATION=cmgfidev

# OPTIONAL: Your Azure DevOps project name
# Leave this BLANK or COMMENT IT OUT to search across ALL PROJECTS in your organization
# Set this to search only one specific project (e.g., "Next Gen LOS")
# NEXT_PUBLIC_ADO_PROJECT=Next Gen LOS

# Personal Access Token (PAT) with Work Items Read permissions
# Create one at: https://cmgfidev.visualstudio.com/_usersSettings/tokens
# Required scopes:
#   - Work Items (Read)
#   - Project and Team (Read) - for all-project searches
ADO_PAT=your-personal-access-token-here

# Note: NEXT_PUBLIC_ variables are exposed to the browser
# ADO_PAT is kept server-side only for security

# ==============================================================================
# AUTHENTICATION CONFIGURATION (Email MFA)
# ==============================================================================
# Vercel KV for OTP storage (required for authentication)
# Setup:
# 1. Go to https://vercel.com/dashboard
# 2. Select your project → Storage tab
# 3. Create new KV database
# 4. Connect it to your project
# 5. Copy these values from Settings → Environment Variables:
KV_REST_API_URL=your-kv-rest-api-url-here
KV_REST_API_TOKEN=your-kv-rest-api-token-here

# JWT Secret for session tokens (generate random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-random-secret-key-here

# SMTP Configuration for sending verification emails
# Use Gmail with App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=markdfm@gmail.com
SMTP_PASS=your-gmail-app-password-here

# ==============================================================================
# QUICK SETUP FOR YOUR ORGANIZATION:
# ==============================================================================
# 1. Copy this file to .env.local
# 2. Keep NEXT_PUBLIC_ADO_ORGANIZATION as: cmgfidev
# 3. REMOVE or COMMENT OUT the NEXT_PUBLIC_ADO_PROJECT line to search ALL projects
# 4. Get your PAT from: https://cmgfidev.visualstudio.com/_usersSettings/tokens
# 5. Paste your PAT in the ADO_PAT line above
# 6. Set up Vercel KV and add REDIS_URL
# 7. Generate JWT_SECRET: openssl rand -base64 32
# 8. Get Gmail App Password and add to SMTP_PASS
